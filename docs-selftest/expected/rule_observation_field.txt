# exit: 1
warning[needs.invalid_field_value]
  --> (need)
      fixtures/rule_observation_field.rst:41:4
  EVD_TOO_LONG:observation: "ubc check was measured at one, five, six, ten and fifty thousand needs across two operating systems, three network conditions and both a cold and a warm licence cache, and every one of those runs is recorded here at a length no bound would allow." is longer than 240 characters (observation/maxLength)
warning[needs.invalid_field_value]
  --> (need)
      fixtures/rule_observation_field.rst:22:4
  EVD_TOO_SHORT:observation: "TBD" is shorter than 20 characters (observation/minLength)
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_observation_field.rst:35:4
  EVD_NBSP:observation: {"pattern":"[^\\x00-\\x7F]"} is not allowed for "The warm index took 41 ms and the cold index took 80 ms." (observation-must-use-only-plain-ascii-characters[34]/allOf/0/properties/observation/not)
    note: The evidence twin of statement-must-use-only-plain-ascii-characters, and the reason both exist as separate rules: they select on a FIELD, and an evidence need has no statement, so without this an observation would be the one normative value in the project still able to hide a non-breaking space. Measured before this rule existed - an observation carrying one produced no diagnostic at all. Same whitelist and same accepted cost as its twin: no micro sign, degree sign or inequality, which bites slightly harder here since measurements are what this field holds. Write microseconds and degrees.
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_observation_field.rst:28:4
  EVD_WRAPPED:observation: {"pattern":"\\n"} is not allowed for "The warm index took 41 ms and the cold index took 80 ms,\nmeasured on this machine." (observation-must-be-one-physical-line[33]/allOf/0/properties/observation/not)
    note: The evidence twin of statement-must-be-one-physical-line, and it needs its own entry because that rule selects on the statement FIELD and an evidence need carries no statement at all. The reason is narrower here: an observation is held to no anchored grammar, so a newline cannot produce the baffling near-miss it produces in a statement. What remains is the honest data constraint - the field's own description promises one line, and these values are read through needs.json and rendered into prompts, where a break inside a sentence is noise.
Found 2 error(s), 2 warning(s).
