# exit: 1
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_statement_shape.rst:30:4
  ARCH_LOWERCASE:statement: "agconflo shall record provenance inside the context store." does not match "^[A-Z][A-Za-z' -]* shall .+\.$" (feat-arch-statement-must-oblige-with-shall[21]/properties/statement/pattern)
    note: A design decision that cannot be stated as an obligation on the system is a description, not a decision.
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_statement_shape.rst:36:4
  DEC_NARRATES:statement: "We evaluated three options and picked the second one." does not match "^[A-Z][A-Za-z' -]* shall .+\.$" (dec-statement-must-oblige-with-shall[39]/properties/statement/pattern)
    note: A decision that narrates - 'we evaluated three options and picked the second' - records the meeting rather than the constraint, and a constraint is the only part worth carrying forward. Held to the same loose shape as stkh_req and feat_arch, a decision carrying no EARS pattern. Note that a superseded or rejected decision states what was PROPOSED rather than what is true of the system; dec_status is the field that tells them apart, and any query over decisions has to filter on it.
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_statement_shape.rst:14:4
  STKH_DESCRIBES:statement: "Agconflo records the provenance of every context." does not match "^[A-Z][A-Za-z' -]* shall .+\.$" (stkh-req-statement-must-oblige-with-shall[20]/properties/statement/pattern)
    note: A stakeholder requirement states a goal rather than system behaviour, so it gets no EARS pattern - but it still has to oblige rather than describe.
Found 3 error(s).
