# exit: 1
warning[needs.invalid_field_value]
  --> (need)
      fixtures/rule_statement_field.rst:28:4
  STKH_TOO_LONG:statement: "Agconflo shall record the provenance of every context, including the node that produced it, the activation it belonged to, the parameters it was bound to, the transform that combined it, and anything else a reader might later wish to know about it." is longer than 240 characters (statement/maxLength)
warning[needs.invalid_field_value]
  --> (need)
      fixtures/rule_statement_field.rst:23:4
  STKH_TOO_SHORT:statement: "It shall work." is shorter than 20 characters (statement/minLength)
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_statement_field.rst:17:4
  STKH_WRAPPED:statement: {"pattern":"\\n"} is not allowed for "Agconflo shall give a node exactly the contexts that were wired\nto it and no others." (statement-must-be-one-physical-line[19]/allOf/0/properties/statement/not)
    note: A wrapped field value keeps a literal newline, which would fail every grammar rule below with a baffling message - and a line break inside a sentence is noise in data that gets rendered into prompts. Selects on the field rather than the type, because this is a property of statements.
error[needs.schema_definition_violation]
  --> (need)
      fixtures/rule_statement_field.rst:17:4
  STKH_WRAPPED:statement: "Agconflo shall give a node exactly the contexts that were wired\nto it and no others." does not match "^[A-Z][A-Za-z' -]* shall .+\.$" (stkh-req-statement-must-oblige-with-shall[20]/properties/statement/pattern)
    note: A stakeholder requirement states a goal rather than system behaviour, so it gets no EARS pattern - but it still has to oblige rather than describe.
Found 2 error(s), 2 warning(s).
